# Demonstrating scaling efficiency with 10 parallel jobs
scaling_test:
  stage: build
  image: alpine:latest
  tags:
    - stress-test
  parallel: 25
  script:
    - echo "Scaling Test - Job $CI_NODE_INDEX of $CI_NODE_TOTAL"
    - echo "System info:"
    - uname -a
    - sleep 30
